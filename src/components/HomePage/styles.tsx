import { makeStyles } from '@material-ui/core';

export const useHomePageStyles = makeStyles(() => ({
  container: {
    height: '100%',
  },
  boardListPaper: {
    width: '50%',
  },
  addNewButton: {
    margin: '15px',
    padding: '15px',
  },
}));
